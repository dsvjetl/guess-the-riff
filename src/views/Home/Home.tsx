import { Box } from '@mui/material';
import { colorTheme } from '../../shared/utils/colorTheme';

const Home = () => {
  return <Box bgcolor={colorTheme.palette.secondary.light}></Box>;
};

export default Home;
